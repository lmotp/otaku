import React from 'react';
import { IMusicQuizInfo } from '../../typings/MusicTestMake';

const YoutubeCard = ({ item }: IMusicQuizInfo) => {
  return <div>YoutubeCard</div>;
};

export default YoutubeCard;
